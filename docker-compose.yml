services:
  vialpilot:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - APP_MODE=production
      - DATABASE_URL=sqlite:///data/vialpilot.db
      - UPLOAD_DIR=data/uploads
    restart: unless-stopped