# VialPilot — all dependencies (Python 3.9+)
# pip install -r requirements.txt

# --- Web API & server ---
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
jinja2>=3.1.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# --- Database ---
sqlalchemy>=2.0.0
greenlet>=3.0.0

# --- LLM (Cerebras Gemma 4) ---
openai>=1.0.0
httpx>=0.27.0

# --- Vision, uploads & video frame extraction ---
pillow>=10.0.0
opencv-python-headless>=4.9.0

# --- Hardware bridge (optional MQTT / webhook) ---
paho-mqtt>=2.0.0

# --- Pipeline Analyzer GUI (optional, disable with ENABLE_PIPELINE_ANALYZER=false) ---
dash>=3.0.0
dash-bootstrap-components>=2.0.0
plotly>=6.0.0
pandas>=2.0.0
requests>=2.28.0

# --- 3D robot simulator (software engine, Windows-safe) ---
gymnasium>=1.0.0
einops
dm-tree
transforms3d
importlib_resources
matplotlib
imageio
tqdm

# --- Testing ---
pytest>=8.0.0

# --- Optional: PyBullet physics (Linux/Docker only) ---
# pip install pybullet>=3.2.5